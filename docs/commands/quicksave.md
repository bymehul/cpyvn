# quicksave (keys)

**Current support**
There are no DSL commands yet for quicksave/quickload. The engine currently provides:

- `F5` quicksave
- `F9` quickload

These store a single `quicksave.json` file in the project's `saves/` folder.
