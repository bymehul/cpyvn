# load

**Syntax**
```vn
load <slot>;
```

**Description**
Loads a save file from the project's `saves/` folder using the slot name. If the slot doesn't exist, nothing happens.

**Example**
```vn
load "chapter1";
```
